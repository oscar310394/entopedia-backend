CREATE TABLE users(id INT AUTO_INCREMENT NOT NULL PRIMARY KEY, name VARCHAR(50) NOT NULL,first_surname VARCHAR(50) NOT NULL, second_surname VARCHAR(50),
phone_number INT NOT NULL, email VARCHAR(50) NOT NULL, occupation VARCHAR(50) NOT NULL, password VARCHAR(50) NOT NULL, is_admi boolean NOT NULL, is_active boolean NOT NULL);

